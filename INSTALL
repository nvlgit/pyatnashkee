Installation Instructions
*************************

Building and Installation
=========================
git clone https://github.com/nvlgit/pyatnashkee.git && cd pyatnashkee
meson builddir --prefix=/usr && cd builddir
ninja
su -c 'ninja install'

Uninstallation
==============
su -c 'ninja uninstall'
